# Node / Capacitor artefacts (generated locally, not tracked)
node_modules/
package-lock.json

# Native shells generated by Capacitor — recreate locally with `npx cap add android` / `cap add ios`
android/
ios/

# Build outputs
dist/
build/
www/

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Local secrets (never commit Supabase/Stripe/VAPID keys)
**/secrets.json
.env
.env.local
